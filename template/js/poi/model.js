define(function() {
  return B.Model.extend({

    /*
     * @id
     * @name (i18n)
     * @description (i18n)
     * @geo
     * @created
     * @updated
     * @thumb
     * @imgs
     * @tags (i18n)
     *
    */

  });
});