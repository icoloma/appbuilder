<div class="row" 
  data-url="{{url}}" >
  <div class="menu-tr-name col-xs-{{poiCount ? 10 : 12}}">{{label}}</div>
  <% if(poiCount) { %>
    <div class="poiCount col-xs-2">{{poiCount}}</div>
  <% } %>
</div>